/** Storage namespace for persisted app data. */
export const STORAGE_KEY = 'habit-tracker-v2';

/** Number of calendar cells (6 weeks). */
export const CALENDAR_CELL_COUNT = 42;

/** Validation constraints for habit names. */
export const HABIT_NAME_MIN_LENGTH = 2;
export const HABIT_NAME_MAX_LENGTH = 60;
